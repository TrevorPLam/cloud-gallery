# Backlog

This backlog captures follow-up tasks discovered during sessions.

## 2026-02-05

- Add AI-META headers to remaining server modules to complete Task 21: `auth-captcha-routes.ts`, `auth-routes.ts`, `db.ts`, `encrypted-storage.ts`, `file-validation.ts`, `index.ts`, `photo-routes.ts`, `storage.ts`, `upload-routes.ts`, plus any remaining test files that should carry metadata.
- Add unit tests for SIEM forwarding (`server/siem.ts`) to validate timeout/endpoint handling.
